rules_version = '2';
// 路線 B：sharedQuizzes 一律由 Cloud Functions（firebase-admin）讀寫，
// admin SDK 走 service account 會跳過 Rules，所以這裡關掉所有 client 直連。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
